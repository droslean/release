apiVersion: apps/v1
kind: Deployment
metadata:
  name: onlinefroutakia-com
  namespace: affwebsites
  labels:
    app: onlinefroutakia-com
spec:
  replicas: 1
  selector:
    matchLabels:
      app: onlinefroutakia-com
  template:
    metadata:
      labels:
        app: onlinefroutakia-com
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: onlinefroutakia_com
        image: ghcr.io/ginbits/beefeater/onlinefroutakia.com:master
        imagePullPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: onlinefroutakia-com
  namespace: affwebsites
spec:
  selector:
    app: onlinefroutakia-com
  ports:
  - name: http
    port: 8080
    protocol: TCP
  type: NodePort
