apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: onlinefroutakia-com-ingress
  namespace: affwebsites
spec:
  rules:
  - host: onlinefroutakia.com
    http:
      paths:
      - backend:
          service:
            name: onlinefroutakia-com
            port:
              number: 8080
        path: /
        pathType: Prefix
